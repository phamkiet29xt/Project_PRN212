<Page x:Class="Project_PRN212.DishList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Project_PRN212"
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="800"
      Title="DishList">

    <Grid Background="White">
        <!-- Header -->
        <Grid Background="#50C4A3" Height="50" VerticalAlignment="Top">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" >
                <Button Content="Profile" Background="#A259D0" Foreground="White" Padding="10" Width="120" Height="40" Margin="0,0,10,0" FontWeight="Bold" />
                <Button Content="Manage Menu" Background="#A259D0" Foreground="White" Padding="10" Margin="0,0,10,0" Width="120" FontWeight="Bold" Click="Menu_Click"/>
                <Button Content="Manage Dish" Background="#A259D0" Foreground="White" Padding="10" Margin="0,0,10,0" Width="120" FontWeight="Bold" />
                <Button Content="Logout" Background="#A259D0" Foreground="White" Padding="10" Width="120" FontWeight="Bold" Click="Logout_Click"/>
            </StackPanel>
        </Grid>

        <!-- Main Content -->
        <StackPanel Margin="20,60,20,10" Background="#FAF9DD" >
            <TextBlock Text="Manage dish" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>

            <!-- Form Input -->
            <Grid Margin="0,0,0,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Name" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" FontWeight="Bold" Margin="10,0,0,0"/>
                <TextBox Grid.Row="0" Grid.Column="1" Height="25" Background="LightGray" Margin="10,10,10,10" Width="500" Name="txtName"/>

                <TextBlock Text="Price" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" FontWeight="Bold" Margin="10,0,0,0"/>
                <TextBox Grid.Row="1" Grid.Column="1" Height="25" Background="LightGray" Width="500" Margin="10,10,10,10" Name="txtPrice"/>

                <TextBlock Text="Description" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" FontWeight="Bold" Margin="10,0,0,0"/>
                <TextBox Grid.Row="2" Grid.Column="1" Height="60" Background="LightGray" Margin="10,10,10,10" Width="500" Name="txtDescription"/>
            </Grid>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
                <Button Content="Refresh" Background="#A259D0" Foreground="White" Width="80" Height="30" Margin="0,0,10,0" Click="Refresh_Click"/>
                <Button Content="Add" Background="#A259D0" Foreground="White" Margin="0,0,10,0" Width="80" Click="Add_Click"/>
                <Button Content="Update" Background="#A259D0" Foreground="White" Margin="0,0,10,0" Width="80" Click="Update_Click"/>
                <Button Content="Delete" Background="#A259D0" Foreground="White" Width="80" Click="Delete_Click"/>
            </StackPanel>

            <!-- Search Bar -->
            <StackPanel Orientation="Horizontal" Margin="0,20,0,10">
                <TextBlock Text="Search by dish name" VerticalAlignment="Center" Margin="13,0,0,0" FontWeight="Bold"/>
                <TextBox Width="200" Margin="20,0,10,0" Name="txtSearch"/>
                <Button Content="Search" Background="#A259D0" Foreground="White" Padding="10" Width="80" Height="36" Margin="10,0,0,0" Click="Search_Click"/>
            </StackPanel>

            <!-- DataGrid -->
            <DataGrid AutoGenerateColumns="False" Height="200" Background="White" Margin="10,0,8,10" SelectionChanged="DataGrid_SelectionChanged" Name="dgDish">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="160"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="120"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Decription}" Width="200"/>
                    <DataGridTextColumn Header="Created By" Binding="{Binding CreatedByNavigation}" Width="170"/>
                    <DataGridTextColumn Header="Created At" Binding="{Binding CreatedAt}" Width="170"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</Page>
